"""Helper functions for DSA problems."""


def print_list(lst):
    """Print a list in a readable format."""
    print(lst)


def print_matrix(matrix):
    """Print a 2D matrix in a readable format."""
    for row in matrix:
        print(row)
